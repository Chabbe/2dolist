const app = require('../app')
const mocha = require('mocha')
const expect = require('chai').expect


describe('Here I describe what the test is', () => {

    it('Should tell me if the functionality works as expected', () => {

        //Setup (t.ex. låtsar variabler för att få ett resultat)

        //Simulate (Har skickar vi med vår setup och simulerar det som ska köras.)

        //Assert

        expect(app).to.exist



    })

})